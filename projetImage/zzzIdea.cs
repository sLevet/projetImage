using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace projetImage
{
    class zzzIdea
    {
        /*
        public void SaveImageInDb22()
        {
            SqlConnection cn = new SqlConnection();             // connection for sql
            try
            {
                //string connectString = "Data Source=WIN-GS9GMUJITS8;Initial Catalog=BDPicture;Integrated Security=True";    // path
                cn.ConnectionString = connectString;
                cn.Open();
                SqlCommand query = new SqlCommand("SELECT name, id FROM T_pictures", cn);
                SqlDataReader dataReader = query.ExecuteReader();    // data reader
                String msg = ""; String oo;
                int tt = 0;
                while (dataReader.Read())
                {
                    msg += ((string)dataReader["name"]);
                    tt = ((int)dataReader["id"]);
                    oo = Convert.ToString(tt);
                    msg += oo;
                }
                MessageBox.Show("dfsaléfjdsafljf" + msg + "--");
                cn.Close();

            }

            catch
            {
                MessageBox.Show("oups.........");
                cn.Close();
            }

        }

        */

    }
}
